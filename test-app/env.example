# Copy this to .env and fill in your Airtable credentials
# The widget will work without these (mock mode) but won't submit to Airtable

VITE_AIRTABLE_API_KEY=your_api_key_here
VITE_AIRTABLE_BASE_ID=your_base_id_here
VITE_AIRTABLE_TABLE_NAME=Feedback
