#!/usr/bin/env python3
# ABOUTME: Empty Python hook for baseline latency measurement
# ABOUTME: Minimal JSON parsing to establish Python interpreter overhead

import json
import sys

json.load(sys.stdin)
sys.exit(0)
